# Noridoc: src

Path: @/src

### Overview

- Core application source for the Reword daily word puzzle game, a Vue 3 single-page app
- Contains game logic (pure functions), audio, analytics, Firebase/leaderboard, PRNG, UI utilities, and all Vue components
- Entry point is `main.js` which initializes GA4 analytics and mounts the Vue app; all game rules live in `game.js`

### How it fits into the larger codebase

- `main.js` mounts the root Vue component from `@/src/components/App.vue`
- `game.js` is the central logic module, imported by both components and `@/tests/` -- it owns puzzle selection, answer validation, tile matching, scoring, streak/lifetime stats, and share text generation
- `prng.js` provides a seeded PRNG. `game.js` uses it in two distinct ways: (1) to deterministically pick offered letters and run the min-answer retry loop, both seeded by the date string (`getDailyRng(dateStr)`); and (2) to build a stable per-bucket shuffle order seeded by a *constant* string, which drives cyclic root selection (see Things to Know). Root words from `@/data/puzzles.json` are chosen by day-index cycling, not by per-day random draws.
- `analytics.js` is a thin system-boundary wrapper around GA4's `gtag.js`. It exports `initGA(measurementId)` (called once by `main.js`) and `trackEvent(eventName, params)` (called by `App.vue` at game events). The module gracefully no-ops when GA is not initialized, so all callers can invoke `trackEvent` unconditionally.
- `firebase.js` initializes the Firebase app and exports a Firestore `db` instance for the `games-reword` project. This is the app's only Firebase entry point; all Firestore operations import `db` from here.
- `leaderboard.js` provides per-round solve rate and per-answer popularity tracking via Firestore. Two functions: `submitGameResults(dateStr, completedRounds)` atomically writes a counter document at `daily/{dateStr}` with `totalGames`, per-round `round{i}Solved` (Firestore `increment()`), and a deep-merged `round{i}Answers` map of the form `{ <answer>: increment(1) }` for each solved round; `fetchSolveRates(dateStr)` reads the counter document and returns `{ rates: number[10], answerCounts: Array<Record<string, number>> }` or `null`. It returns `null` both when the document does not exist and when `totalGames < 5` (privacy/noise gate -- breakdowns appear only after a daily puzzle has at least 5 submissions). Both are called by `App.vue` at game end -- submit is fire-and-forget, fetch populates `solveRates` for `ScoreScreen` and `answerCounts` for `WordListModal`. The Firestore security rules (`@/firestore.rules`) enforce that `totalGames` can only increment by 1 per write and whitelist exactly the `totalGames`, `round{0..9}Solved`, and `round{0..9}Answers` field set on both create and update.
- `sound.js` is a browser-side audio utility consumed by components. `ui.js` is a standalone legacy vanilla JS UI implementation (predating the Vue rewrite) that is not imported by any file in the project; it remains in the tree but is effectively dead code
- `@/style.css` at the project root contains all CSS, including component-specific styles (no scoped styles in `.vue` files). All tile racks (gameplay `.tile-rack`, input area, and modal `.modal-tile-rack`) share a common responsive pattern: `flex-wrap: nowrap` with `flex: 0 1 <basis>` and `aspect-ratio: 1`, so tiles shrink to fit narrow viewports rather than wrapping onto new lines. The `@media (max-width: 420px)` breakpoint overrides `flex-basis` for each rack type. The modal tile rack (`.modal-tile-rack .tile`) additionally overrides two inherited base `.tile` properties: `margin: 0` (the base `.tile` has `margin: 3px`, which wastes significant horizontal space on long words) and `min-width: 0` (overrides flexbox's default `min-width: auto`, which otherwise prevents tiles from shrinking below their content size). Without these overrides, words of 9+ letters overflow the modal on mobile viewports.
- `@/tests/components.test.js` tests both the pure game logic and the Vue components

### Core Implementation

- **Puzzle data model**: Each puzzle round has a `root` word, `expansions` (a map of letter-key to valid answer words), a `trivialAnswers` array of words that are trivial morphological inflections of the root (pre-computed at build time by `@/scripts/build-words.js`), and `offeredLetters` (the extra letters presented to the player, generated at runtime by `getOfferedLetters()` using the seeded PRNG, not stored in the puzzle data file). Words in `trivialAnswers` are already excluded from `expansions` -- the two are disjoint sets.
- **`completedRounds` data structure**: The central state for a finished game. Each entry contains `{ root, answer, timeMs, offeredLetters, possibleAnswers, hinted }`. This is persisted to localStorage and consumed by `ScoreScreen` and `WordListModal`. Old saved games that lack `offeredLetters` or `hinted` are backfilled (`offeredLetters` from puzzle data on load, `hinted` defaults to `false`); see `App.vue` onMounted.
- **`matchTypedToTiles()`**: Given typed letters, root letters, and offered letters, produces a matched array where each letter is tagged as `root`, `offered`, or `invalid`. Prioritizes matching against root letters first. Used by `GameBoard` for live input display and by `WordListModal` to color-code possible answer tiles.
- **`removeLetterAt(letters, index)`**: Pure function that returns a new array with the element at the given index removed. Used by `App.vue` to support click-to-remove on filled input tiles (position-specific removal, as opposed to `processKeyPress` Backspace which only removes the last letter).
- **`getAnswersForRound()`**: Collects all valid answers by iterating expansion keys that are subsets of the offered letters. Since the puzzle data's `expansions` are already pre-filtered to exclude trivial inflections at build time, this function does not perform any runtime trivial filtering. Then performs a stable partition: words present in `round.commonWords` are placed before all other words. If `commonWords` is absent or empty, original iteration order is preserved. Both the skip-round hint (up to 3 answers) and the score screen word list consume this function's output, so this is the single sort point for common word priority.
- **Profanity check**: `isProfane(word)` uses the `bad-words` npm package (`Filter.isProfane()`) to detect profane input at runtime. A single `Filter` instance is created at module load. This is the runtime complement to the build-time profanity filter in `@/scripts/build-words.js` -- build-time filtering removes profane words from puzzle data, while runtime filtering catches profane user input that would otherwise pass validation.
- **Answer validation**: `isValidAnswer()` checks that the answer exists in expansions whose keys are subsets of offered letters. It does not filter trivial inflections -- that filter ran at build time and trivials are stored separately in `round.trivialAnswers`.
- **Trivial inflection check**: `isTrivialAnswer(answer, round)` returns true if the answer is in `round.trivialAnswers` (case-insensitive). Tolerates missing/empty `trivialAnswers`. Used by `getSubmitFeedbackType` to return the `'trivial-suffix'` feedback type when a player types a trivial inflection of the root (e.g. `startled` for root `startle`).
- **Feedback type priority**: `getSubmitFeedbackType()` checks profanity first (before length, correctness, or trivial checks), so profane input is always rejected with the `'profanity'` type regardless of whether it would be a valid game answer.
- **Hint selection**: `getHintLetter(round)` picks the best offered letter to reveal as a hint. It prefers offered letters that appear as single-char `commonKeys` entries, breaking ties by counting how many `commonWords` that letter's expansion produces. If no offered letter is a common key, it falls back to the offered letter with the most total expansion words. Returns `null` if no offered letters have any expansions.
- **Share text**: `generateShareText()` maps each round to an emoji: `🟩` (solved), `🟡` (hinted and solved), or `⬜` (skipped). Skip takes precedence over hint (empty answer = skipped regardless of hint state). The stats line includes solve count, total letter count across all answers (singular "letter" when exactly 1, plural "letters" otherwise), and elapsed time when timer is enabled, pipe-delimited. Always ends with a `rewordgame.xyz` site link line.
- **"All solved" vs "perfect"**: Two distinct concepts. `isAllSolved(results)` returns true when all 10 rounds have non-empty answers -- hints are allowed, only skips disqualify. This is used to trigger the confetti celebration in `ScoreScreen`. In contrast, `isPerfect` (computed inside `updateLifetimeStats()`) requires all 10 rounds solved with no skips, no hints, and timer enabled. `isPerfect` affects `fastestTimeMs` and `perfectGamesPlayed` tracking.
- **Lifetime stats**: `updateLifetimeStats()` tracks `totalHints` across games.

### Things to Know

- **Cyclic root selection**: `selectDailyPuzzle(puzzleData, dateStr)` chooses the day's roots by deterministic cycling, not by per-day random draws. Each length bucket (`"3"`, `"4"`, `"5"`, `"6"`, plus a combined `"7+"` pool) is placed into a fixed shuffled order via `cyclicSelection`, which calls `seededShuffle` seeded by a *constant* string (e.g. `'reword-cycle-order-3'`). That order is stable across all dates and is intentionally decoupled from the quality-sorted order in `@/data/puzzles.json`. The day's roots are a consecutive window into that order at `start = (dayIndex * count) mod poolSize`, where `dayIndex` comes from `dayIndexFromDateStr(dateStr)` (days since the Unix epoch, UTC). The round structure is fixed at 2/3/3/1/1 (lengths 3/4/5/6/7+). The `"7+"` pool combines all length>=7 entries in numeric-ascending key order before its stable shuffle. Because the bucket order is fixed and roots are drawn as a sliding window, a root is never reused until its whole bucket is exhausted (the prior independent per-day shuffles caused birthday-paradox repeats within ~9 days). `selectDailyPuzzle` remains a pure function of `(puzzleData, dateStr)`: same date => same puzzle.
- **Minimum answer guarantee**: After roots are chosen, `selectDailyPuzzle()` picks offered letters via `getOfferedLetters(entry, rng)` using the date-seeded `rng` (`getDailyRng(dateStr)`), retrying (up to `MAX_OFFERED_LETTER_RETRIES` attempts) when `getAnswersForRound()` returns fewer than `MIN_ANSWERS_PER_ROUND` (3) valid answers. It keeps the best attempt (most answers) as a fallback if no attempt meets the minimum. Offered letters are still entirely date-RNG-driven; only the bucket ORDER is seeded by a constant. `MIN_ANSWERS_PER_ROUND` is a plain module-level `const` (not exported); tests inline the literal `3`.
- **Regeneration caveat**: The stable bucket order and the `dayIndex -> window` mapping both depend on the current contents of `@/data/puzzles.json`. If that file is later regenerated with different roots (e.g. via `npm run build-words`), the shuffled order and the start offset for a given day both shift, which can cause a one-time repeat or skip of roots around the regeneration date. This is an accepted tradeoff; no epoch or version anchor was built into the cycle.
- **Deploy-day leaderboard blemish**: Switching to (or away from) cyclic selection changes both the day's roots and its offered letters for *every* date, so the very first deploy of this algorithm has the same one-time shift as a regeneration. Because the Firestore leaderboard doc is keyed only by `dateStr` (`daily/{dateStr}`), on the transition day players who finished before the deploy recorded answers for the old puzzle and players after the deploy record answers for the new puzzle into the *same* document, so that one day's `round{i}Answers` popularity map and `round{i}Solved` rates mix two puzzles. It is self-healing (every subsequent day is clean) and only affects the per-answer breakdown / solve-rate stats, never gameplay. Deploying near 00:00 UTC minimizes the split.
- `offeredLetters` is always exactly 3 letters, generated from expansion keys plus random alphabet fill. `getOfferedLetters()` uses a priority hierarchy for the first letter: (1) a single-letter key from `commonKeys` (expansion keys leading to common words), (2) any letter extracted from multi-letter common keys, (3) fallback to any valid expansion key if `commonKeys` is absent or empty. The `commonKeys` field is expected to come from the puzzle data; if absent, the function falls back to the pre-existing behavior of picking any valid expansion letter.
- Trivial inflection filtering happens at build time in `@/scripts/build-words.js` using `wink-lemmatizer` to detect morphological relationships (plurals, past tense, comparatives, superlatives, present participles) between each expansion word and its root. The runtime `isTrivialAnswer(answer, round)` simply consults the pre-computed `round.trivialAnswers` list, which is the single source of truth for whether a typed word is a trivial inflection. The runtime game logic does not re-derive this; it trusts the build output.
- The `expansions` map keys are letter combinations (e.g., "e", "el"), not full words -- `isKeySubsetOfOffered()` checks if the key's letters are all present in the offered set
- The `commonKeys` and `commonWords` fields from puzzle data are used both at puzzle generation time (for biasing offered letters) and at runtime (for hint selection via `getHintLetter`). These fields are optional; code falls back gracefully when they are absent.
- Timer gives 70s on touch devices vs 60s on pointer devices, detected once via `matchMedia` at module load. The per-round `state.roundDeadline` is set by `startTimer()` in `@/src/components/App.vue` and is also mutated mid-round by the wrong-answer time-penalty path in `handleSubmit()` (see `@/src/components/docs.md`). Code reasoning about the timer must treat `roundDeadline` as input-mutable, not write-once.
- `analytics.js` uses `window.dataLayer.push(arguments)` (not spread) as the boundary contract with GA4's `gtag.js`. The gtag.js script is loaded asynchronously via a `<script async>` tag in `@/index.html` with the measurement ID `G-B61TJ0H3MM` hardcoded (it is inherently public). `initGA` is called before `createApp()` in `main.js`.
- **Firestore data model**: One document per day at `daily/{dateStr}`. Fields are `totalGames` (number of players who finished), `round0Solved`..`round9Solved` (number of players who solved each round), and `round0Answers`..`round9Answers` (per-round map of `{ <chosen answer>: count }`). All writes use `setDoc` with `merge: true` and Firestore `increment()`, so the document is created on first write and atomically updated thereafter; nested `round{i}Answers` maps are deep-merged so each submission only bumps the count of the specific word the player chose. This is the app's only network dependency; all Firebase calls are wrapped in `.catch(() => {})` at the call site in `App.vue`, so network failures are silently swallowed and the game functions identically offline.
- **`<5 games` gate**: `fetchSolveRates` returns `null` whenever `totalGames < 5`, even if the document exists. This is the single gate that hides both the per-round solve percentages on `ScoreScreen` and the per-answer popularity breakdown in `WordListModal` until enough players have completed the daily puzzle. The threshold was lowered from 10 to 5 because daily completion counts hover around 6-11, making stats rarely visible at the higher threshold. The same threshold is intentionally shared by both UIs.

Created and maintained by Nori.
